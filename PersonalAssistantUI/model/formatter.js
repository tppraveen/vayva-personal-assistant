sap.ui.define([], () => {
	"use strict";

	return {
     
		  fnSingleBooleanCheck: function(bLoaded) { // no need for ref only
          return !!bLoaded; // ensures a proper boolean
      }
     
	 
		 

	};
});